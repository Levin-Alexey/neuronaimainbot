/**
 * ROI калькулятор
 */
import { sendMessage } from '../../services/telegram.js';

export async function handleROICalculator(chatId, env) {
  const message = `<b>📈 ROI калькулятор</b>

🚧 <i>Раздел в разработке</i>

Здесь вы сможете рассчитать окупаемость инвестиций в AI:

• Период окупаемости проекта
• Прогнозируемая прибыль
• Сравнение сценариев "до" и "после"
• Долгосрочная выгода

<b>Скоро здесь появится интерактивный калькулятор ROI!</b>`;

  const keyboard = {
    inline_keyboard: [
      [
        { text: '⬅️ Назад к калькуляторам', callback_data: 'benefits' }
      ],
      [
        { text: '🏠 Главное меню', callback_data: 'back' }
      ]
    ]
  };

  return sendMessage(chatId, message, env, keyboard);
}
