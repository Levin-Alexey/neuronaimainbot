/**
 * Калькулятор экономии
 */
import { sendMessage } from '../../services/telegram.js';

export async function handleSavingsCalculator(chatId, env) {
  const message = `<b>📊 Калькулятор экономии</b>

🚧 <i>Раздел в разработке</i>

Здесь вы сможете рассчитать потенциальную экономию от внедрения AI-решений:

• Снижение затрат на персонал
• Ускорение бизнес-процессов
• Уменьшение операционных расходов
• Повышение эффективности работы

<b>Скоро здесь появится интерактивный калькулятор!</b>`;

  const keyboard = {
    inline_keyboard: [
      [
        { text: '⬅️ Назад к калькуляторам', callback_data: 'benefits' }
      ],
      [
        { text: '🏠 Главное меню', callback_data: 'back' }
      ]
    ]
  };

  return sendMessage(chatId, message, env, keyboard);
}
