/**
 * Обработчик раздела "Посчитать выгоду от AI"
 */
import { sendMessage } from '../services/telegram.js';

export async function handleBenefitsFlow(chatId, env) {
  const message = `<b>💰 Посчитать выгоду от AI</b>

Выберите инструмент для расчета:

<b>📊 Калькулятор экономии</b>
Рассчитайте, сколько вы сможете сэкономить на автоматизации процессов с помощью AI

<b>📈 ROI калькулятор</b>
Узнайте окупаемость инвестиций в AI-решения для вашего бизнеса`;

  const keyboard = {
    inline_keyboard: [
      [
        { text: '📊 Калькулятор экономии', callback_data: 'calc_savings' }
      ],
      [
        { text: '📈 ROI калькулятор', callback_data: 'calc_roi' }
      ],
      [
        { text: '⬅️ Назад', callback_data: 'back' }
      ]
    ]
  };

  return sendMessage(chatId, message, env, keyboard);
}
